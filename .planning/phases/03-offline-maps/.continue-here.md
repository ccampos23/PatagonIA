---
context: phase
phase: 03-offline-maps
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-06-20T00:13:13.460Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: org.json on JVM — `org.json.JSONObject` is stubbed in Android unit tests and throws RuntimeException. — Use `testImplementation("org.json:json:20231013")` in build.gradle.kts to supply a pure JVM implementation.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| org.json RuntimeException in unit tests | Running tests that parse GeoJSON using org.json without a JVM-compatible implementation throws RuntimeException | advisory | testImplementation("org.json:json:20231013") is added to build.gradle.kts |

<current_state>
We are currently in Phase 03 (Offline Maps), Plan 02 (Offline Map Downloader Service and Asset Manager UI).
Task 1 (Offline Map Downloader Service) is completed and committed in `c236e34`.
Task 2 (Asset Manager Screen) is partially started with the creation of the untracked UI state (`AssetManagerUiState.kt`) and test class (`AssetManagerViewModelTest.kt`). We need to implement `AssetManagerViewModel.kt` and `AssetManagerScreen.kt`.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Offline Map Downloader Service - Done (committed as `c236e34`)
  - Foreground service `TileDownloadService` with `dataSync` type, notification channel, throttle updates.
  - Android 15 `onTimeout` handling.
  - Start/Pause/Resume/Cancel intent actions.
  - Park presets, State Machine (`TileDownloadState`), exponential retry backoff, repository interface.
  - 32 unit tests passing.
</completed_work>

<remaining_work>

- Task 2: Asset Manager Screen (TDD) - In progress
  - Create `AssetManagerViewModel.kt` that implements the logic required by `AssetManagerViewModelTest.kt`.
  - Create `AssetManagerScreen.kt` UI with Compose.
  - Wire UI to start/stop the service, show park presets and custom bounding boxes, show size warnings (>100MB), and enforce hard tile limit blocks (>750k tiles).
  - Run tests to verify.
</remaining_work>

<decisions_made>

- Add test dependency on `org.json:json:20231013` to resolve JVM stub exceptions for `org.json.JSONObject` in `TrailGeoJsonParserTest`.
- Design `AssetManagerUiState` as an immutable data class to maintain unidirectional data flow for the Asset Manager screen.
</decisions_made>

<blockers>
- Mapbox API token (`MAPBOX_DOWNLOADS_TOKEN`) is missing. Mapbox dependency is commented in `build.gradle.kts` and placeholders are used in code. The Service uses placeholder code for TileStore interaction.
</blockers>

## Required Reading (in order)
1. [.planning/phases/03-offline-maps/03-02-PLAN.md](file:///home/pingu/Projects/PatagonIA/.planning/phases/03-offline-maps/03-02-PLAN.md) — Plan details for current tasks.
2. [app/src/test/java/com/patagonia/app/presentation/viewmodel/AssetManagerViewModelTest.kt](file:///home/pingu/Projects/PatagonIA/app/src/test/java/com/patagonia/app/presentation/viewmodel/AssetManagerViewModelTest.kt) — Current failing/unimplemented test suite that defines VM behavior.
3. [app/src/main/java/com/patagonia/app/presentation/viewmodel/AssetManagerUiState.kt](file:///home/pingu/Projects/PatagonIA/app/src/main/java/com/patagonia/app/presentation/viewmodel/AssetManagerUiState.kt) — Current UI state class for the Asset Manager.

## Infrastructure State
- Gradle Java version uses JBR from Android Studio (`/home/pingu/Downloads/android-studio/jbr`). Needs `export JAVA_HOME=/home/pingu/Downloads/android-studio/jbr` before running gradle commands.

<context>
The goal of this phase is offline maps. We've built the map UI with placeholder and the offline tile downloader service. Now we need the UI screen (Asset Manager Screen) to manage download requests and display existing downloaded areas.
</context>

<next_action>
Start with: Implement `AssetManagerViewModel.kt` in `app/src/main/java/com/patagonia/app/presentation/viewmodel/` to satisfy the tests in `AssetManagerViewModelTest.kt`.
</next_action>
