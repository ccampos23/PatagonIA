---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/build.gradle.kts, local.properties, app/src/main/java/com/patagonia/app/PatagoniaApplication.kt, app/src/main/java/com/patagonia/app/presentation/map/MapScreen.kt]
autonomous: true
requirements: [REQ-CORE-01]

must_haves:
  truths:
    - User can see a map using Mapbox Outdoors style
    - Trail lines render over the map from GeoJSON
    - Species captures are displayed as custom pins
  artifacts:
    - path: app/src/main/java/com/patagonia/app/presentation/map/MapScreen.kt
      provides: Core Map UI
    - path: app/build.gradle.kts
      provides: Mapbox Compose dependencies
  key_links:
    - from: MapScreen.kt
      to: Mapbox MapView
      via: AndroidView wrapper
---

<objective>
Integrate Mapbox Maps SDK and build the core map interface.
Purpose: Provide the foundational map view that renders outdoor trekking routes and personal captures.
Output: Working MapScreen with Suda-style trail lines and custom sighting pins.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-offline-maps/03-CONTEXT.md
@.planning/phases/03-offline-maps/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Mapbox Dependencies</name>
  <files>app/build.gradle.kts, local.properties, app/src/main/AndroidManifest.xml</files>
  <action>Add Mapbox Maps Compose extension dependencies to `app/build.gradle.kts`. Enable `buildConfig = true`. Add the Mapbox public token to `local.properties` (or inject via CI) so it is compiled into `BuildConfig.MAPBOX_DOWNLOADS_TOKEN` (per D-09). Add necessary Internet and Location permissions to AndroidManifest.xml.</action>
  <verify>
    <automated>./gradlew :app:assembleDebug</automated>
  </verify>
  <done>Project builds successfully with Mapbox dependencies and BuildConfig field available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MapScreen Wrapper</name>
  <files>app/src/main/java/com/patagonia/app/presentation/map/MapScreen.kt</files>
  <behavior>
    - Test 1: MapScreen renders AndroidView successfully
    - Test 2: Mapbox MapView initializes with Mapbox Outdoors style (D-10)
  </behavior>
  <action>Create the MapScreen composable. Wrap the native Mapbox MapView inside an AndroidView (per D-08). Initialize the map to load the Outdoors style. Ensure it renders correctly when offline with a blank grid if tiles are missing (per D-11).</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest</automated>
  </verify>
  <done>MapScreen component exists and unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Render Trails and Pins</name>
  <files>app/src/main/java/com/patagonia/app/presentation/map/MapScreen.kt</files>
  <action>Integrate GeoJSON parsing to render custom trekking trails as dashed lines (per D-13, D-14). Fetch Species captures and map them to custom category-specific icons on the map (per D-19). Implement the My Journal / Global Sightings toggle logic (per D-20).</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest</automated>
  </verify>
  <done>Trails and pins overlay correctly on the MapView.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Network -> Map SDK | Mapbox tile downloads |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | Map SDK | mitigate | Use HTTPS for all Mapbox API calls automatically |
</threat_model>

<verification>
Automated unit tests for MapScreen logic pass.
</verification>

<success_criteria>
Map renders successfully with Outdoors style, rendering trail paths and sighting pins.
</success_criteria>

<output>
Create `.planning/phases/03-offline-maps/03-01-SUMMARY.md` when done
</output>
