---
phase: 03
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [app/src/main/java/com/patagonia/app/domain/services/OfflineMapDownloaderService.kt, app/src/main/java/com/patagonia/app/presentation/map/AssetManagerScreen.kt, app/src/main/AndroidManifest.xml]
autonomous: true
requirements: [REQ-CORE-04]

must_haves:
  truths:
    - User can trigger a map region download
    - Download runs in a foreground service
    - Progress is reported to the user
  artifacts:
    - path: app/src/main/java/com/patagonia/app/domain/services/OfflineMapDownloaderService.kt
      provides: Background downloading logic
    - path: app/src/main/java/com/patagonia/app/presentation/map/AssetManagerScreen.kt
      provides: UI for managing offline maps
  key_links:
    - from: AssetManagerScreen.kt
      to: OfflineMapDownloaderService
      via: StartService Intent
---

<objective>
Implement the Offline Map Downloader Service and Asset Manager UI.
Purpose: Allow users to pre-download mapping data for specific regions before losing connectivity.
Output: Working Foreground service with notifications and an Asset Manager Screen.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-offline-maps/03-CONTEXT.md
@.planning/phases/03-offline-maps/03-RESEARCH.md
@.planning/phases/03-offline-maps/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Offline Map Downloader Service</name>
  <files>app/src/main/java/com/patagonia/app/domain/services/OfflineMapDownloaderService.kt, app/src/main/AndroidManifest.xml</files>
  <behavior>
    - Test 1: Service requests POST_NOTIFICATIONS permission logic
    - Test 2: Service creates notification channel
  </behavior>
  <action>Implement `OfflineMapDownloaderService` as a Foreground Service using type `dataSync` (per D-16). Implement Mapbox TileStore logic to download `TileRegion`. Handle Android 15 `onTimeout` to save progress (per D-17). Add `<service>` to `AndroidManifest.xml`.</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest</automated>
  </verify>
  <done>Service is declared and unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Asset Manager Screen</name>
  <files>app/src/main/java/com/patagonia/app/presentation/map/AssetManagerScreen.kt</files>
  <behavior>
    - Test 1: Screen displays list of downloaded regions
    - Test 2: Screen allows starting a new region download
  </behavior>
  <action>Create `AssetManagerScreen` UI. List existing downloaded regions and provide options for downloading presets (Chilean parks) or custom bounding boxes (per D-15). Warn if download exceeds 100MB (per D-18). Wire UI to start the `OfflineMapDownloaderService`.</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest</automated>
  </verify>
  <done>Asset Manager UI renders and test suites pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Network -> Disk | Map tile storage |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Denial of Service | TileStore | mitigate | Enforce 100MB warnings and hard limit of 750k tiles to prevent storage exhaustion |
</threat_model>

<verification>
Unit tests for service and asset manager pass.
</verification>

<success_criteria>
Users can successfully queue and view progress of map region downloads.
</success_criteria>

<output>
Create `.planning/phases/03-offline-maps/03-02-SUMMARY.md` when done
</output>
