---
phase: 03
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified: [app/src/main/java/com/patagonia/app/presentation/map/MapScreen.kt, app/src/main/java/com/patagonia/app/domain/services/OfflineMapDownloaderService.kt, app/src/main/java/com/patagonia/app/domain/model/SightingCluster.kt]
autonomous: true
requirements: [REQ-CORE-01, REQ-CORE-04]

must_haves:
  truths:
    - User can toggle compass heading behavior
    - Tapping a pin shows a tooltip popup
    - Sighting clusters hide sparse data and show density ranges
    - Map storage is properly constrained to internal storage
  artifacts:
    - path: app/src/main/java/com/patagonia/app/domain/model/SightingCluster.kt
      provides: Clustering logic for crowdsourced sightings
  key_links:
    - from: MapScreen.kt
      to: Mapbox TileStore configuration
      via: Cache config
---

<objective>
Implement advanced map interactions and configure storage & caching policies.
Purpose: Polish the map experience with tooltips and compass, while ensuring map data is securely and efficiently stored according to user decisions.
Output: Refined MapScreen and properly configured TileStore cache.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-offline-maps/03-CONTEXT.md
@.planning/phases/03-offline-maps/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map Interactions and Tooltips</name>
  <files>app/src/main/java/com/patagonia/app/presentation/map/MapScreen.kt</files>
  <behavior>
    - Test 1: Compass toggle triggers heading vs north-up change
    - Test 2: Tapping pin displays species tooltip
  </behavior>
  <action>Implement a user-controlled compass toggle (North-up vs Heading-up) (per D-12). Add an anchored popup tooltip when tapping a map pin showing basic info (species name, thumbnail) (per D-23).</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest</automated>
  </verify>
  <done>Compass toggle and popup tooltip function as expected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sighting Clustering</name>
  <files>app/src/main/java/com/patagonia/app/domain/model/SightingCluster.kt, app/src/main/java/com/patagonia/app/presentation/map/MapScreen.kt</files>
  <behavior>
    - Test 1: Sparse sightings (count < 10) are filtered out
    - Test 2: Sightings are grouped into density ranges
  </behavior>
  <action>Filter crowdsourced global sightings to hide pins when the count is < 10 (per D-21). Configure visual ranges for density clusters using easily changeable limits (e.g., 10-50, 50-300, 300-1000+) (per D-22).</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest</automated>
  </verify>
  <done>Sighting clusters filter appropriately and render visually distinct ranges.</done>
</task>

<task type="auto">
  <name>Task 3: Storage and Caching Config</name>
  <files>app/src/main/java/com/patagonia/app/PatagoniaApplication.kt, app/src/main/java/com/patagonia/app/domain/services/OfflineMapDownloaderService.kt</files>
  <action>Configure the Mapbox TileStore initialization to store all downloaded map data in internal storage (`context.filesDir`) only (per D-24). Set up the hybrid caching model with a 250MB ambient caching limit and protect explicitly downloaded offline regions from auto-eviction (per D-25).</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest</automated>
  </verify>
  <done>TileStore is configured with proper internal storage path and 250MB quota.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Network -> Disk | Map tile storage |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | Map Storage | mitigate | Restrict all map storage to internal storage (context.filesDir) |
</threat_model>

<verification>
Unit tests for clustering logic pass, and storage initialization succeeds without crashing.
</verification>

<success_criteria>
All remaining Context decisions (Compass, Popup, Clusters, and Storage config) are implemented and functional.
</success_criteria>

<output>
Create `.planning/phases/03-offline-maps/03-03-SUMMARY.md` when done
</output>
