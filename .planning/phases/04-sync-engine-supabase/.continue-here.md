---
context: phase
phase: 04-sync-engine-supabase
task: 2
total_tasks: 4
status: in_progress
last_updated: 2026-07-07T02:08:18.570Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: Supabase BOM 3.6.0 incompatible with Kotlin 2.1 — pin BOM to 3.1.1 + force Ktor 3.0.0 — already mitigated, do NOT bump BOM back to 3.6.0 without first upgrading Kotlin to 2.3+, AGP to 8.9.1+, compileSdk to 36+. Rule 4.
- [x] CONSTRAINT: androidx.browser transitive 1.10.0 incompatible with compileSdk 35 — force-pin to 1.8.0 — already mitigated. Do NOT remove the `configurations.all { resolutionStrategy.force("androidx.browser:browser:1.8.0") }` block in app/build.gradle.kts without first bumping compileSdk.
- [ ] CONSTRAINT: Do NOT attempt to spawn gsd-executor subagent — the agent type is not installed in this opencode workspace (init reported `agents_installed=false`). Continue using sequential inline execution per execute-plan.md until /gsd-install-agents is run.

**Do not proceed until the third box is checked (acknowledged).**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Guessing Supabase SDK API signatures | Used `io.github.jan.supabase.auth.signInWith` import paths that don't exist as top-level extensions; built wrong API surface for `signUpWith` and `data` lambda type. | advisory | Introspect the actual JAR via `javap -classpath auth-kt-api.jar -public io.github.jan.supabase.auth.Auth` before writing any Supabase SDK call. Confirmed: `Auth.signInWith/signUpWith/signOut` are instance methods (no top-level extension functions), `Email.Config.data` is `JsonObject` (not `Map<String,String>`), `signUpWith` returns `UserInfo?`, `currentUserOrNull()` returns `UserInfo?`. |
| Skipping per-task pause checkpoints | User explicitly requested pausing after every task. Default GSD executor runs all 4 tasks of a plan one-shot. | advisory | After every task commit, stop, summary, and ask user before starting next task. This overrides the inline-execution default. |
| Writing local.properties via Edit tool | A prior session in this workspace accidentally blanked out `local.properties` (lost `sdk.dir` and Mapbox token) — gradlew then failed with "SDK location not found". | advisory | Treat `local.properties` as critical state. Always re-check its contents after any tool that might touch it; never overwrite without preserving `sdk.dir`, `MAPBOX_ACCESS_TOKEN`, and the Supabase entries. |

<current_state>
Mid-execution of Plan 04-01 (phase 04 - sync-engine-supabase), inside execute-phase's sequential inline fallback. Tasks 1 and 2 are committed and verified; next is Task 3 (TDD: AuthRepository + UseCases). Per user preference, paused at the post-Task-2 checkpoint awaiting go-ahead.
</current_state>

<completed_work>

### Plan 04-01 — Task 1: Add Supabase SDK + WorkManager deps (DONE — commit c54d8e6)

- `app/build.gradle.kts`:
  - Added `kotlin("plugin.serialization")` plugin
  - Added Supabase BOM 3.1.1 (NOT 3.6.0 as plan said — Rule 2 deviation, precleared)
  - Added `auth-kt`, `postgrest-kt`, `storage-kt`
  - Added `work-runtime-ktx:2.10.0`, `hilt-work:1.2.0`, `hilt-compiler:1.2.0`
  - Upgraded Ktor 2.3.12 -> 3.0.0, swapped `ktor-client-okhttp` -> `ktor-client-android`
  - Added BuildConfig fields `SUPABASE_URL`, `SUPABASE_ANON_KEY` from local.properties
  - Added `configurations.all { resolutionStrategy.force }` block pinning androidx.browser:1.8.0 and Ktor sub-artifacts to 3.0.0
- `build.gradle.kts` (root): added `org.jetbrains.kotlin.plugin.serialization` 2.1.0 to classpath
- `app/.../di/NetworkModule.kt`: OKHttp engine -> Android engine; still provides `Json` configured with `ignoreUnknownKeys`/`prettyPrint`/`coerceInputValues`
- `app/.../data/remote/KtorModelDownloader.kt`: fixed `onDownload { bytesSentTotal, contentLength -> }` callback for Ktor 3.0's nullable `Long?` parameters (defensive null-checks)
- Verify: `./gradlew :app:assembleDebug` -> BUILD SUCCESSFUL in 2m 6s

### Plan 04-01 — Task 2: SupabaseModule + SupabaseAuthDataSource TDD (DONE — RED f60107b + GREEN c880d4a)

- `app/.../domain/model/UserProfile.kt` — domain model (id / email / username / avatarUrl / bio / level / isPrivate). Stub for Task 2; full bio/level/isPrivate semantics exercised by Task 3.
- `app/.../domain/repository/AuthException.kt` — domain-level auth failure exception (D-24 inline errors)
- `app/.../data/remote/SupabaseAuthApi.kt` — thin wrapper interface around Supabase Auth plugin (Rule 2 deviation for testability — extracted because the SDK DSL is hard to mock)
- `app/.../data/remote/SupabaseAuthApiImpl.kt` — Supabase-backed impl:
  - `signInWithEmailAndPassword(email, password)` calls `auth.signInWith(Email) { email=...; password=... }`, then reads `currentUserOrNull()` and maps to UserProfile
  - `signUpWithEmailAndPassword(email, password, username)` calls `auth.signUpWith(Email) { email=...; password=...; data=buildJsonObject { put("username", JsonPrimitive(username)) } }` (username in user_metadata for D-17 uniqueness), maps returned UserInfo to UserProfile with fallback to currentUserOrNull
  - `currentSession()` reads `auth.currentUserOrNull()?.toUserProfile()` (nullable snapshot)
  - `signOut()` calls `auth.signOut()` (default scope)
  - API verified via jar introspection (`javap` on `auth-kt-api.jar`)
- `app/.../data/remote/SupabaseAuthDataSource.kt` — facade wrapping SupabaseAuthApi; try/catch translates any throwable to `AuthException` (D-24)
- `app/.../di/SupabaseModule.kt` — Hilt module: `@Provides @Singleton SupabaseClient` using `createSupabaseClient(BuildConfig.SUPABASE_URL, BuildConfig.SUPABASE_ANON_KEY) { install(Auth); install(Postgrest) }`; `provideAuth`, `providePostgrest`; `@Binds SupabaseAuthApi -> SupabaseAuthApiImpl`
- `app/src/test/java/com/patagonia/app/data/remote/SupabaseAuthDataSourceTest.kt` — 6 unit tests, all passing (login success, login fail -> AuthException, register success, register dup-email -> AuthException, currentSession null, logout delegation)
- Verify: `./gradlew :app:testDebugUnitTest --tests "*SupabaseAuthDataSourceTest*"` -> BUILD SUCCESSFUL, 6/6 tests pass (0 failures, 0 errors)

### Tracking commits

- `26090dd chore(04): begin phase execution tracking` — STATE.md + config.json sync
- `c54d8e6 feat(04-01): add Supabase SDK + WorkManager deps, Ktor 3.0 upgrade`
- `f60107b test(04-01): add failing tests for SupabaseAuthDataSource (RED)`
- `c880d4a feat(04-01): implement SupabaseModule + SupabaseAuthDataSource (GREEN)`

</completed_work>

<remaining_work>

### Plan 04-01 — Task 3 (NOT STARTED) — TDD: AuthRepository + UserProfile + Auth UseCases

Files to create/modify per plan:
- `app/.../domain/repository/AuthRepository.kt` (interface — login/register/logout/observeSession/getCurrentUser/isUsernameAvailable)
- `app/.../data/repository/AuthRepositoryImpl.kt` (binds SupabaseAuthDataSource)
- `app/.../domain/usecase/auth/LoginUseCase.kt`
- `app/.../domain/usecase/auth/RegisterUseCase.kt`
- `app/.../domain/usecase/auth/GetSessionUseCase.kt`
- `app/.../domain/usecase/auth/LogoutUseCase.kt`
- `app/.../data/di/RepositoryModule.kt` (add `@Binds AuthRepository`)

TDD order:
1. RED: write failing tests for each use case (LoginUseCase success/failure, RegisterUseCase username check + dup-email, GetSessionUseCase Flow emission, LogoutUseCase clears session)
2. GREEN: implement AuthRepository interface + AuthRepositoryImpl + 4 use cases
3. REFACTOR: cleanup, deduplicate

Verify: `./gradlew :app:testDebugUnitTest --tests "*auth*"`

### Plan 04-01 — Task 4 (NOT STARTED) — TDD: Auth Presentation Layer

Files per plan: AuthViewModel, LoginScreen, RegisterScreen composables, MainActivity auth-gate refactor (replacing the `showMainApp` boolean with a session check). WARNING: MainActivity.kt is also modified by Plan 04-03 in its Task 4 ("Wire Sync Status into UI"). Do NOT parallelize — sequential inline only. The auth gate (Task 4 here) must commit first; plan 04-03 will layer on top later.

### Plan 04-01 — SUMMARY.md (NOT CREATED)

Only after all 4 tasks complete.

### Plans 04-02 and 04-03 (NOT STARTED — out of scope per user's "execute plan 01 only" choice)

Per `.planning/phases/04-sync-engine-supabase/04-02-PLAN.md` and `04-03-PLAN.md` — both blocked on Plan 04-01. To run them later: `/gsd-execute-phase 04`. They will auto-skip Plan 04-01 (since 04-01-SUMMARY.md will exist) and execute 04-02 + 04-03 sequentially.

</remaining_work>

<decisions_made>

- **Supabase BOM 3.1.1 instead of 3.6.0** — precleared with user. BOM 3.6.0 brings Ktor 3.4.3 compiled with Kotlin 2.3 metadata; project is Kotlin 2.1. Bumping the toolchain (Kotlin 2.3 + AGP 8.9.1 + compileSdk 36) is Rule 4 and out of scope. Force-pin Ktor sub-artifacts to 3.0.0 so the BOM cannot override.
- **androidx.browser 1.8.0 force-pin** — precleared with user. Supabase transitive 1.10.0 requires compileSdk 36 + AGP 8.9.1; project on 35 + 8.7.3.
- **Extracted `SupabaseAuthApi` interface** — design adjustment (Rule 2). The plan said `SupabaseAuthDataSource` injected with `SupabaseClient`, but the SDK DSL is hard to mock. Thin wrapper keeps the data source unit-testable without leaking SDK types.
- **Ktor OkHttp engine -> Android engine** — plan-spec (not a deviation). Plan said to swap.

</decisions_made>

<blockers>

- **SUPABASE_URL placeholder** — `local.properties` has `SUPABASE_URL=https://your-project.supabase.co`. Anon key is set (`sb_publishable_KU6j_8fxi-pKJA4VrRnw0Q_pcv7UgyU`). User must paste the real Supabase project URL before runtime tests. WORKAROUND if URL is still missing: do not attempt `login()`/`register()` integration tests against Supabase — unit tests use mocks and are unaffected. Task 3 (pure domain layer) is unblocked.
- **No gsd-executor subagent installed** — opencode workspace agent runtime reported `agents_installed=false`. Cannot use `Agent(subagent_type="gsd-executor")`. Continue inline per execute-plan.md.
- **Pre-existing uncommitted `opencode.json` diff** — noticed at the start of this session; pre-existing, unrelated to Phase 04 work. Left un-staged intentionally. Do NOT commit it.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable state (this file's structured companion)
2. `.planning/phases/04-sync-engine-supabase/04-01-PLAN.md` — the plan being executed; tasks 3 and 4 still pending
3. `.planning/phases/04-sync-engine-supabase/04-CONTEXT.md` — locked implementation decisions (D-01..D-27)
4. `.opencode/gsd-core/workflows/execute-plan.md` — the inline TDD cycle RULE: RED commit -> GREEN commit -> REFACTOR; per-task commit granularity
5. `.planning/PROJECT.md` — core value: offline-first nature journal
6. `.planning/STATE.md` — current project state (status: Executing Phase 04)
7. `AGENTS.md` — TDD enforcement rules
8. Existing app conventions to mimic:
   - `app/.../domain/repository/CaptureRepository.kt` (interface pattern)
   - `app/.../data/repository/CaptureRepositoryImpl.kt` (impl pattern)
   - `app/.../domain/usecase/AddCaptureUseCase.kt` (use-case `operator fun invoke` pattern)
   - `app/.../data/di/RepositoryModule.kt` (Hilt `@Binds` pattern — extend this module, don't create a new one)
   - `app/src/test/.../presentation/viewmodel/MapViewModelTest.kt` (test conventions: mockito-kotlin, Dispatchers.setMain, StandardTestDispatcher)

## Infrastructure State

- Build tooling: Kotlin 2.1.0, AGP 8.7.3, JDK 17, compileSdk 35, minSdk 26
- Supabase Kotlin SDK 3.1.1 (auth-kt, postgrest-kt, storage-kt on classpath — storage installed later)
- Ktor 3.0.0 (Android engine)
- WorkManager 2.10.0 + Hilt-Work 1.2.0 (on classpath — used in Plan 04-03)
- Hilt 2.54, Room 2.6.1, Compose BOM 2024.12.01
- local.properties (gitignored): sdk.dir, MAPBOX_ACCESS_TOKEN, SUPABASE_URL (placeholder), SUPABASE_ANON_KEY (real)
- No running background processes
- All work committed on branch `camilo` (NOT a phase branch — `branching_strategy: none`)

## Pre-Execution Critique Required

(none — execution already in progress; Tasks 1-2 reviewed and verified)

<context>
Plan 04-01 has 4 tasks. Tasks 1-2 are done and verified by `./gradlew`. User chose "execute plan 01 only, then stop" and additionally requested per-task pause checkpoints. After all 4 tasks of plan 04-01 complete, write 04-01-SUMMARY.md and stop. Plans 04-02 and 04-03 are deliberately out-of-scope for this run. TDD is enforced by `workflow.tdd_mode=true`; Task 3 must be RED-commit-first.
</context>

<next_action>
Resume with Task 3 TDD cycle:
1. RED: write `app/src/test/java/com/patagonia/app/domain/usecase/auth/{LoginUseCaseTest,RegisterUseCaseTest,GetSessionUseCaseTest,LogoutUseCaseTest}.kt` (6 tests per plan) that reference not-yet-existing `AuthRepository`, `LoginUseCase`, `RegisterUseCase`, `GetSessionUseCase`, `LogoutUseCase`. Run tests -> confirm they fail to compile. Commit as `test(04-01): add failing tests for auth use cases (RED)`.
2. GREEN: implement `AuthRepository` interface, `AuthRepositoryImpl` (delegating to `SupabaseAuthDataSource`), `RepositoryModule` `@Binds` for AuthRepository, and the 4 use cases. Run tests -> confirm all pass. Commit as `feat(04-01): implement AuthRepository + auth use cases (GREEN)`.
3. REFACTOR: review for duplication, extract shared logic. If no changes needed, skip refactor commit. Otherwise: `refactor(04-01): clean up auth use cases`.
4. After Task 3 commits, PAUSE and ask user before starting Task 4.
</next_action>
