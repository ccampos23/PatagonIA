---
phase: 04
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [app/src/main/java/com/patagonia/app/data/local/entity/CaptureEntity.kt, app/src/main/java/com/patagonia/app/domain/model/Capture.kt, app/src/main/java/com/patagonia/app/domain/model/SyncStatus.kt, app/src/main/java/com/patagonia/app/data/local/dao/CaptureDao.kt, app/src/main/java/com/patagonia/app/data/local/PatagoniaDatabase.kt, app/src/main/java/com/patagonia/app/data/local/converter/SyncStatusConverter.kt, app/src/main/java/com/patagonia/app/data/remote/SupabaseCaptureDataSource.kt, app/src/main/java/com/patagonia/app/data/repository/CaptureRepositoryImpl.kt, app/src/main/java/com/patagonia/app/domain/repository/CaptureRepository.kt, app/src/main/java/com/patagonia/app/presentation/profile/ProfileScreen.kt, app/src/main/java/com/patagonia/app/presentation/profile/ProfileViewModel.kt, app/src/main/java/com/patagonia/app/presentation/settings/SettingsScreen.kt]
autonomous: true
requirements: [REQ-SOC-01, REQ-SOC-02]

must_haves:
  truths:
    - CaptureEntity has sync_status, remote_id, is_shared, is_deleted, and user_id fields (D-11, D-12, D-13)
    - Room database migrates from v1 to v2 without data loss
    - Supabase captures table schema matches local entity with RLS enabled (D-20)
    - Keep coordinates exact when sharing a capture (D-15)
    - Profile-level privacy toggle exists and controls capture sharing (D-14)
    - User profile screen displays username, avatar, bio, and level (D-17, D-18, D-19)
    - Supabase remote data source can CRUD captures via Postgrest
  artifacts:
    - path: app/src/main/java/com/patagonia/app/domain/model/SyncStatus.kt
      provides: SyncStatus enum for tracking capture sync state
    - path: app/src/main/java/com/patagonia/app/data/remote/SupabaseCaptureDataSource.kt
      provides: Remote CRUD for captures via Supabase Postgrest
    - path: app/src/main/java/com/patagonia/app/presentation/profile/ProfileScreen.kt
      provides: User profile UI with privacy toggle
  key_links:
    - from: CaptureEntity.kt
      to: SyncStatus enum
      via: TypeConverter
    - from: SupabaseCaptureDataSource.kt
      to: Supabase Postgrest
      via: SupabaseClient injection
---

<objective>
Set up Supabase database schema, evolve local Room schema for sync tracking, and build the user profile experience.
Purpose: Prepare both local and remote data layers so the sync engine (Plan 03) has matching schemas and clear sync state tracking.
Output: Migrated Room DB with sync fields, Supabase remote CRUD, and profile/settings screens.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sync-engine-supabase/04-CONTEXT.md
@.planning/phases/04-sync-engine-supabase/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Room Schema with Sync Fields</name>
  <files>app/src/main/java/com/patagonia/app/data/local/entity/CaptureEntity.kt, app/src/main/java/com/patagonia/app/domain/model/Capture.kt, app/src/main/java/com/patagonia/app/domain/model/SyncStatus.kt, app/src/main/java/com/patagonia/app/data/local/converter/SyncStatusConverter.kt, app/src/main/java/com/patagonia/app/data/local/PatagoniaDatabase.kt, app/src/main/java/com/patagonia/app/data/local/dao/CaptureDao.kt</files>
  <behavior>
    - Test 1: SyncStatus enum contains PENDING_INSERT, PENDING_UPDATE, SYNCED, PENDING_DELETE, SYNC_FAILED values (D-11)
    - Test 2: SyncStatusConverter correctly converts between enum and string for Room
    - Test 3: CaptureEntity with new sync fields can be inserted and queried from Room
    - Test 4: CaptureDao.getPendingSyncCaptures returns only non-SYNCED captures
    - Test 5: CaptureDao.markAsSynced updates sync_status to SYNCED and sets remote_id
    - Test 6: Room migration from v1 to v2 preserves existing data, replaces isSynced=true with SYNCED and isSynced=false with PENDING_INSERT
  </behavior>
  <action>
Create `SyncStatus` enum:
```
enum class SyncStatus { PENDING_INSERT, PENDING_UPDATE, SYNCED, PENDING_DELETE, SYNC_FAILED }
```

Create `SyncStatusConverter` TypeConverter for Room.

Extend `CaptureEntity` — **replace existing `isSynced: Boolean` with richer sync tracking**:
- Remove `isSynced: Boolean` field
- Add `syncStatus: SyncStatus = SyncStatus.PENDING_INSERT` (D-11)
- Add `remoteId: String? = null` (UUID from Supabase — for linking local ↔ remote records)
- Add `isShared: Boolean = false` (private by default per Discussion Log)
- Add `isDeleted: Boolean = false` (D-12 — soft deletes)
- Add `userId: String? = null` (D-20 — Supabase Auth UUID foreign key)

Update `Capture` domain model to mirror the new fields (also remove `isSynced`).
Update `CaptureEntityMapper` to handle the renamed/new fields.

Update `CaptureDao`:
- Replace `getUnsyncedCaptures()` with `getPendingSyncCaptures(): Flow<List<CaptureEntity>>` — WHERE syncStatus != 'SYNCED'
- Add `markAsSynced(id: String, remoteId: String)` — sets syncStatus = SYNCED
- Add `markAsSyncFailed(id: String)` — sets syncStatus = SYNC_FAILED (D-22)
- Add `getByRemoteId(remoteId: String): CaptureEntity?`
- Add `softDelete(id: String)` — sets isDeleted = true, syncStatus = PENDING_DELETE (D-12)

Bump `PatagoniaDatabase` to version 2 with `MIGRATION_1_2` that:
1. Adds new columns: `syncStatus TEXT DEFAULT 'SYNCED'`, `remoteId TEXT`, `isShared INTEGER DEFAULT 0`, `isDeleted INTEGER DEFAULT 0`, `userId TEXT`
2. Migrates existing `isSynced` data: `UPDATE captures SET syncStatus = CASE WHEN isSynced = 1 THEN 'SYNCED' ELSE 'PENDING_INSERT' END`
3. Drops the `isSynced` column (or keeps for backward compat and ignores via @Ignore)

Register `SyncStatusConverter` in the database `@TypeConverters`.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*SyncStatus*" --tests "*CaptureDao*"</automated>
  </verify>
  <done>Room schema v2 includes sync tracking fields, migration passes, and new DAO queries return correct data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Supabase Remote Capture Data Source</name>
  <files>app/src/main/java/com/patagonia/app/data/remote/SupabaseCaptureDataSource.kt, app/src/main/java/com/patagonia/app/data/remote/dto/RemoteCaptureDto.kt</files>
  <behavior>
    - Test 1: insertCapture sends correct fields to Supabase and returns remote UUID
    - Test 2: updateCapture updates an existing remote capture by ID
    - Test 3: softDeleteCapture sets is_deleted = true on the remote record (D-12)
    - Test 4: fetchAllCaptures retrieves all captures for the current user (D-25)
    - Test 5: updateSharingStatus immediately updates the is_shared flag remotely (D-16)
  </behavior>
  <action>
Create `RemoteCaptureDto` as a `@Serializable` data class matching the Supabase table schema:
- `id: String` (UUID, primary key)
- `user_id: String` (FK to auth.users — D-20)
- `species_name: String`
- `image_path: String`
- `latitude: Double` (keep coordinates exact, no obfuscation — D-15)
- `longitude: Double` (keep coordinates exact, no obfuscation — D-15)
- `captured_at: Long` (timestamp)
- `confidence: Float`
- `notes: String`
- `is_shared: Boolean` (D-13)
- `is_deleted: Boolean` (D-12)
- `created_at: String` (server-managed)
- `updated_at: String` (server-managed)

Create `SupabaseCaptureDataSource` (injected with `SupabaseClient`):
- `suspend fun insertCapture(dto: RemoteCaptureDto): String` — inserts via Postgrest, returns UUID.
- `suspend fun updateCapture(dto: RemoteCaptureDto)` — updates by id.
- `suspend fun softDeleteCapture(remoteId: String)` — sets is_deleted = true (D-12).
- `suspend fun fetchAllCaptures(userId: String): List<RemoteCaptureDto>` — fetches all for user (D-25).
- `suspend fun updateSharingStatus(remoteId: String, isShared: Boolean)` — toggles sharing immediately (D-16).

Document the expected Supabase SQL schema as comments in the class:
```sql
CREATE TABLE captures (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) NOT NULL,
  species_name TEXT NOT NULL,
  image_path TEXT NOT NULL,
  latitude DOUBLE PRECISION NOT NULL,
  longitude DOUBLE PRECISION NOT NULL,
  captured_at BIGINT NOT NULL,
  confidence REAL NOT NULL,
  notes TEXT DEFAULT '',
  is_shared BOOLEAN DEFAULT false,
  is_deleted BOOLEAN DEFAULT false,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
ALTER TABLE captures ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can CRUD own captures" ON captures
  FOR ALL TO authenticated
  USING ((select auth.uid()) = user_id)
  WITH CHECK ((select auth.uid()) = user_id);
```
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*SupabaseCaptureDataSourceTest*"</automated>
  </verify>
  <done>Remote data source can insert, update, soft-delete, and fetch captures with proper serialization. Tests pass with mocked Supabase client.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update CaptureRepository for Sync-Aware Operations</name>
  <files>app/src/main/java/com/patagonia/app/domain/repository/CaptureRepository.kt, app/src/main/java/com/patagonia/app/data/repository/CaptureRepositoryImpl.kt, app/src/main/java/com/patagonia/app/domain/usecase/SaveCaptureUseCase.kt, app/src/main/java/com/patagonia/app/domain/usecase/DeleteCaptureUseCase.kt</files>
  <behavior>
    - Test 1: saveCapture sets syncStatus to PENDING_INSERT for new captures
    - Test 2: updateCapture sets syncStatus to PENDING_UPDATE for existing captures
    - Test 3: deleteCapture soft-deletes and sets syncStatus to PENDING_DELETE (D-12)
    - Test 4: getCaptures excludes soft-deleted captures from the list
    - Test 5: toggleSharing updates isShared and sets syncStatus to PENDING_UPDATE (D-16)
  </behavior>
  <action>
Update `CaptureRepository` interface:
- `suspend fun saveCapture(capture: Capture)` — now sets syncStatus = PENDING_INSERT.
- `suspend fun updateCapture(capture: Capture)` — sets syncStatus = PENDING_UPDATE.
- `suspend fun deleteCapture(id: String)` — soft-delete: sets isDeleted = true, syncStatus = PENDING_DELETE (D-12).
- `fun getCaptures(): Flow<List<Capture>>` — filter WHERE isDeleted = false.
- `suspend fun toggleSharing(id: String, isShared: Boolean)` — updates isShared, sets syncStatus = PENDING_UPDATE (D-16).
- `fun getPendingSyncCaptures(): Flow<List<Capture>>` — exposes non-SYNCED captures (replaces existing `getUnsyncedCaptures()`).

Update `CaptureRepositoryImpl` to implement the new methods using the updated `CaptureDao`.

Update `SaveCaptureUseCase` and `DeleteCaptureUseCase` to use the new sync-aware repository methods.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*CaptureRepository*" --tests "*SaveCaptureUseCase*" --tests "*DeleteCaptureUseCase*"</automated>
  </verify>
  <done>Repository and use case tests pass with sync-aware behavior for all CRUD operations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Profile and Settings Screens</name>
  <files>app/src/main/java/com/patagonia/app/presentation/profile/ProfileScreen.kt, app/src/main/java/com/patagonia/app/presentation/profile/ProfileViewModel.kt, app/src/main/java/com/patagonia/app/presentation/settings/SettingsScreen.kt, app/src/main/java/com/patagonia/app/presentation/settings/SettingsViewModel.kt, app/src/main/java/com/patagonia/app/domain/usecase/profile/UpdateProfileUseCase.kt, app/src/main/java/com/patagonia/app/presentation/navigation/NavHost.kt</files>
  <behavior>
    - Test 1: ProfileViewModel loads current user profile from AuthRepository
    - Test 2: ProfileViewModel displays username, bio, level, and avatar (D-17, D-18, D-19)
    - Test 3: SettingsViewModel toggles privacy from public to private and persists (D-14)
    - Test 4: SettingsViewModel provides a logout action that clears session
    - Test 5: ProfileViewModel shows sync error indicator when SYNC_FAILED captures exist (D-21)
  </behavior>
  <action>
Create `ProfileViewModel`:
- Load current user profile from `AuthRepository.getCurrentUser()`.
- Expose `ProfileUiState` with username, bio, level, avatarUrl, captureCount, syncErrorCount.
- Show sync error indicator (subtle warning icon) when any captures have `SYNC_FAILED` status (D-21).

Create `ProfileScreen` composable:
- Display avatar (circular image with fallback initial), username, bio, level badge (D-17, D-18, D-19).
- Show total captures count.
- Show sync status indicator (D-21) — subtle warning icon if sync errors exist.
- Navigation to settings.
- Material 3 theming with green brand palette.

Create `SettingsViewModel`:
- Privacy toggle (isPrivate) — profile-level privacy setting (D-14).
- Logout action via `LogoutUseCase`.
- Manual "Retry Sync" button visibility when SYNC_FAILED captures exist (D-23).

Create `SettingsScreen` composable:
- Account section: email (read-only), username.
- Privacy toggle: Switch to make account private/public (D-14).
- Logout button.
- Sync section: "Retry Sync" button if sync errors exist (D-23).

Create `UpdateProfileUseCase` to handle profile updates (bio, privacy toggle).

Update `NavHost`:
- Add `profile` and `settings` routes.
- Add profile icon to bottom nav or top bar.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*ProfileViewModel*" --tests "*SettingsViewModel*"</automated>
  </verify>
  <done>Profile and Settings screens render with user data, privacy toggle persists, and sync error indicator functions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Local Room DB → Supabase | Data schema must match between local and remote |
| App → Supabase Storage | Avatar upload to Supabase Storage buckets |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | Room Migration | mitigate | Test migration v1→v2 with existing data; default sync_status to SYNCED for pre-existing captures |
| T-04-06 | Information Disclosure | Captures RLS | mitigate | Enable RLS with user_id = auth.uid() policy; users only see their own captures |
| T-04-07 | Elevation of Privilege | Public captures | mitigate | Profile-level privacy toggle controls visibility; immediate removal when toggled off (D-16) |
| T-04-08 | Spoofing | Profile | mitigate | Force unique usernames at database level with UNIQUE constraint (D-17) |
</threat_model>

<verification>
All Room migration, DAO, data source, repository, use case, and ViewModel tests pass.
Schema between Room and Supabase is consistent.
</verification>

<success_criteria>
Room DB v2 tracks sync state for every capture. Supabase remote data source performs CRUD. Profile screen shows user info with sync error indicator. Privacy toggle controls sharing. All unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/04-sync-engine-supabase/04-02-SUMMARY.md` when done
</output>
